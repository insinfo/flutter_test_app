import 'package:flutter/material.dart';

// Colors
const scaffoldBackgroundColor = Color.fromRGBO(241, 240, 242, 1);

// Fonts
final iconFont = 'pmro_app';

const double drawerHeaderHeight = 160.0 + 1.0;
const Color drawerColor = Color.fromRGBO(0, 20, 150, 0.1);
const appBarDarkColor = Color(0xff2E363B);

// Text Style
const appBarTitleTextStyle = TextStyle(
  fontFamily: 'Poppins',
  fontWeight: FontWeight.bold,
  color: Colors.white,
  fontSize: 14,
);

/*
ThemeData appTheme() {
  return ThemeData(
    primaryColor: Colors.white,
    accentColor: Colors.orange,
    hintColor: Colors.white,
    dividerColor: Colors.white,
    buttonColor: Colors.white,
    scaffoldBackgroundColor: Colors.black,
    canvasColor: Colors.black,
  );
} */
